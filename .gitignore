# Secrets / environment
.env
.env.*
!.env.example

# Logs / run outputs
logs/

# Customer data — never commit
testing/samples/

# Internal bank source documents — never commit.
# The routing matrix is transcribed into src/matrix/document_matrix.yaml, which
# IS tracked because the code cannot load without it; the source workbook stays
# local. Keep the two in sync by hand.
docs/*.xlsx
docs/*.xls
# Screenshots and scans dropped in here as design references are real customer
# material — business models, partner names, revenue shares, warehouse sites.
docs/*.jpg
docs/*.jpeg
docs/*.png
docs/*.pdf

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# OCR / caches
.cache/
*.tmp

# OS / editor
.DS_Store
.vscode/
.idea/

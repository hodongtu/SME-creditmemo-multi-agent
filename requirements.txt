# SME Credit Memo — Python dependencies
# Install: pip install -r requirements.txt
# System requirements (not pip):
#   - Tesseract OCR — see README.md
#   - WeasyPrint native libs (Pango/Cairo/GLib), for PDF export:
#     macOS:  brew install pango cairo glib
#     Debian: apt-get install libpango-1.0-0 libcairo2 libgdk-pixbuf2.0-0

# --- Agent framework ---
langgraph==1.2.6
langchain==1.3.11
langchain-core==1.4.8
langchain-openai==1.3.2
openai==2.43.0

# --- Document extraction & OCR ---
pypdfium2==5.12.1
pytesseract==0.3.13
opencv-python==4.13.0.92
numpy==2.4.6
pillow==12.2.0
pandas==2.3.3
openpyxl==3.1.5          # xlsx reading (direct, format-faithful)
xlrd==2.0.2              # legacy .xls reading via pandas
markdown==3.10.3         # Markdown -> HTML for PDF export
weasyprint==69.0         # HTML -> PDF (needs Pango/Cairo/GLib, see above)

# --- Runtime / notebook ---
python-dotenv==1.2.2
nest-asyncio==1.6.0
ipython==9.14.1

# --- Optional ---
langchain-tavily==0.2.18  # web search enrichment (RUN_WEB_SEARCH=true)
langsmith==0.8.18         # tracing (LANGSMITH_TRACING=true)
